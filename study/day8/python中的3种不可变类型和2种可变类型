python中的不可变类型(但都可以作为字典中的key)
    1.数字
    2.字符串
        例如：
            a="zxc"
            a="Zxc" 这里看似修改，实际只是引用的位置的指向发生了变化，并非修改。切记
    3.元组


pythonz中的可变类型（不能作为字典中的key）
    1.列表
        例如：
            a=[11,22,33]
            a[0]=zxc
            a=[zxc,22,33]
    2.字典
        例如：
            b={"name":"laowang"}
            b["name"]=laoli
            b={"name":"laoli"}