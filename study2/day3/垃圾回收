垃圾回收
    小整数对象池
        python对小整数的定义是-5--267之间的整数对象是提前建立好的，不会被垃圾回收（常驻内存）
    大整数对象池
        每一个大整数，均创建一个新的对象。使用完毕后，python解释器会从内存中销毁


        intern共享机制：创建多个对象时，若对象都相同，且无特殊字符，符号的共用一份

python清理内存,以引用计数方式为主,以隔代回收为辅

引用计数:有变量指向是引用计数加1
    导致应用计数+1
        1.对象被创建,例如:a=1
        2.对象被引用b=a
        3.对象被作为参数,传到另一个函数中,func(a)
        4.对象作为一个元素,存储在容器中,list=[a,b,c]

    导致引用计数-1
        1.直接销毁对象:del a
        2.a=2,变量重新引用了一个对象
        3.对象离开了他的作用域
        4.对象所在的容器被销毁

查看对象的引用计数的个数
    sys.getrefcount(a)

隔代回收(GC)
    创建完对象,会将对象挂到链表上

    第一条链表：1<-->1<-->3<-->2<-->3
        执行到一定程度第一条集体在引用的计数机制上减一,为0的清除,不为0的移到第二条链表

    第二条链表:2<-->1<-->2
        执行到一定程度第二条集体在引用的计数机制上减一,为0的清除,不为0的移到第三条链表

    第三条链表:1<-->1

    注:在减一之前,python会判断是否是循环引用
    循环引用:没有其他变量指向引用,两个对象自身相互引用