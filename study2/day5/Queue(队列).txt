Queue队列

	Queue(3)：创建一个队列

	put("str"):向队列中添加一条消息（数据类型没有限制）

	qsize():查看队列中的消息数

	get():从队列中取出一条消息（先进先出）

	empty():查看队列是否为空

	full():查看队列是否已满

	get_nowait():获取不等待，当在队列中取消息时，如果队列为空，系统一直等待，这个方法是让系统不等待，直接结束

	put_nowait():添加不等待，当向队列中添加消息时，如果队列已满，系统一直等待，这个方法是让系统不等待，直接结束